# Captured Parity Reference

This directory retains the exact compiled JavaScript and CSS bundles recovered from the source-of-truth live deployment capture. They are reference artifacts only and are not executed by the clean React application.
