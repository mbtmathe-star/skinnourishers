import React, { useMemo, useState } from 'react';
import { AnimatePresence, motion } from 'framer-motion';
import { ChevronLeft, ChevronRight, MessageCircle, X } from 'lucide-react';
import Layout from '../components/Layout';
import PageHero from '../components/PageHero';
import results from '../data/results.json';

const categories = ['All', 'Acne', 'Pigmentation', 'Anti-Aging', 'Body'];
export default function Results() {
  const [category, setCategory] = useState('All'); const [selected, setSelected] = useState(null);
  const filtered = useMemo(() => category === 'All' ? results : results.filter((item) => item.category === category), [category]);
  const current = selected === null ? null : filtered[selected];
  const next = () => setSelected((selected + 1) % filtered.length); const prev = () => setSelected((selected - 1 + filtered.length) % filtered.length);
  return <Layout><PageHero tagline="Real Client Transformations" title="Before &" titleHighlight="After" subtitle="See the real results our clients achieve with professional, science-backed treatments." secondaryButtonText="View Services" secondaryButtonLink="/services" imageIndex={4} /><section className="sticky top-16 z-40 bg-background/95 backdrop-blur-lg border-b border-border/50 py-4"><div className="container"><div className="flex gap-2 overflow-x-auto pb-1 scrollbar-hide">{categories.map((item) => <button key={item} onClick={() => { setCategory(item); setSelected(null); }} className={`whitespace-nowrap px-5 py-2 rounded-full text-sm font-medium transition-all ${category === item ? 'bg-primary text-primary-foreground shadow-lg' : 'bg-muted text-muted-foreground hover:bg-muted/80'}`}>{item}</button>)}</div></div></section><section className="py-16 lg:py-24"><div className="container"><motion.div className="grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5" layout><AnimatePresence mode="popLayout">{filtered.map((item, index) => <motion.div key={item.image} layout initial={{ opacity: 0, scale: .9 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: .9 }} transition={{ duration: .3 }} className="group relative cursor-pointer" onClick={() => setSelected(index)}><div className="relative overflow-hidden rounded-2xl bg-card shadow-elegant hover:shadow-xl transition-all duration-500 hover:-translate-y-1"><div className="relative aspect-[3/4] overflow-hidden"><img src={item.image} alt={item.caption} className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" loading="lazy" /><div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/10 to-transparent opacity-60 group-hover:opacity-80 transition-opacity" /><div className="absolute inset-0 flex items-center justify-center bg-primary/20 opacity-0 group-hover:opacity-100 transition-opacity"><div className="w-14 h-14 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center"><span className="text-white font-semibold text-sm">View</span></div></div></div></div></motion.div>)}</AnimatePresence></motion.div><p className="text-center text-sm text-muted-foreground mt-10">*Individual results may vary. These are real client transformations.</p></div></section><section className="py-20 bg-gradient-to-br from-primary via-primary to-accent relative overflow-hidden"><div className="container text-center relative z-10"><h2 className="font-heading text-3xl md:text-4xl lg:text-5xl font-semibold text-primary-foreground mb-6">Ready for Your Transformation?</h2><p className="text-primary-foreground/80 text-lg md:text-xl mb-10 max-w-2xl mx-auto">Chat with a skin specialist to discuss your goals and see what's possible.</p><a href="https://wa.me/27783003706?text=Hi%2C%20I%27d%20like%20to%20discuss%20my%20skin%20concerns%20and%20see%20what%20results%20I%20can%20achieve" target="_blank" rel="noreferrer" className="inline-flex items-center justify-center h-11 px-10 bg-[#25D366] hover:bg-[#20BA5C] rounded-full shadow-2xl text-base text-white"><MessageCircle className="h-5 w-5 mr-2" />Chat With a Specialist</a></div></section><AnimatePresence>{current && selected !== null && <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-black/90 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={() => setSelected(null)}><button onClick={() => setSelected(null)} className="absolute top-6 right-6 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center hover:bg-white/20 transition-colors z-10"><X className="w-6 h-6 text-white" /></button><button onClick={(e) => { e.stopPropagation(); prev(); }} className="absolute left-4 md:left-8 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center hover:bg-white/20 transition-colors z-10"><ChevronLeft className="w-6 h-6 text-white" /></button><button onClick={(e) => { e.stopPropagation(); next(); }} className="absolute right-4 md:right-8 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center hover:bg-white/20 transition-colors z-10"><ChevronRight className="w-6 h-6 text-white" /></button><motion.div className="max-w-4xl mx-4" initial={{ scale: .9, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} exit={{ scale: .9, opacity: 0 }} onClick={(e) => e.stopPropagation()}><img src={current.image} alt={current.caption} className="w-full max-h-[80vh] object-contain rounded-2xl" /></motion.div></motion.div>}</AnimatePresence></Layout>;
}
