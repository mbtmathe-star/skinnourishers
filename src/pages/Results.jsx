import React, { useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { AnimatePresence, motion } from 'framer-motion';
import { ArrowRight, ChevronLeft, ChevronRight, X } from 'lucide-react';
import Layout from '../components/Layout';
import PageHero from '../components/PageHero';
import results from '../data/results.json';

const categories = ['All', 'Acne', 'Pigmentation', 'Anti-Aging', 'Body'];
export default function Results() {
  const [category, setCategory] = useState('All'); const [selected, setSelected] = useState(null);
  const filtered = useMemo(() => category === 'All' ? results : results.filter((item) => item.category === category), [category]);
  const current = selected === null ? null : filtered[selected];
  const next = () => setSelected((selected + 1) % filtered.length); const prev = () => setSelected((selected - 1 + filtered.length) % filtered.length);
  return <Layout><PageHero tagline="Real Client Transformations" title="Before &" titleHighlight="After" subtitle="See the real results our clients achieve with professional, science-backed treatments." secondaryButtonText="View Services" secondaryButtonLink="/services" imageIndex={4} /><section className="sticky top-16 z-40 bg-background/95 backdrop-blur-lg border-b py-4" style={{ borderColor: 'var(--line)' }}><div className="container"><div className="flex gap-2 overflow-x-auto pb-1 scrollbar-hide">{categories.map((item) => <button key={item} onClick={() => { setCategory(item); setSelected(null); }} className="whitespace-nowrap px-5 py-2 font-mono uppercase transition-all" style={{ borderRadius: '2px', fontSize: '11px', letterSpacing: '.06em', background: category === item ? 'hsl(var(--primary))' : 'hsl(var(--muted))', color: category === item ? 'hsl(var(--primary-foreground))' : 'var(--fg-muted)' }}>{item}</button>)}</div></div></section><section className="sec-porcelain sec-pad"><div className="container"><motion.div className="grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5" layout><AnimatePresence mode="popLayout">{filtered.map((item, index) => <motion.div key={item.image} layout initial={{ opacity: 0, scale: .9 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: .9 }} transition={{ duration: .3 }} className="group relative cursor-pointer" onClick={() => setSelected(index)}><div className="relative overflow-hidden transition-all duration-500 hover:-translate-y-1" style={{ border: '1px solid var(--line)', borderRadius: '2px' }}><div className="relative aspect-[3/4] overflow-hidden"><img src={item.image} alt={item.caption} className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" loading="lazy" /><div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/10 to-transparent opacity-60 group-hover:opacity-80 transition-opacity" /><div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity" style={{ background: 'hsl(var(--primary) / .2)' }}><div className="w-12 h-12 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center"><span className="text-white font-mono text-xs uppercase" style={{ letterSpacing: '.08em' }}>View</span></div></div></div></div></motion.div>)}</AnimatePresence></motion.div><p className="text-center text-sm mt-10" style={{ color: 'var(--fg-muted)' }}>*Individual results may vary. These are real client transformations.</p></div></section><section className="sec-ink sec-pad relative overflow-hidden"><div className="container text-center relative z-10"><span className="eyebrow mb-2" style={{ display: 'inline-block' }}>Your turn</span><h2 className="d2 sec-h">Ready for your transformation?</h2><p className="lede sec-p" style={{ marginInline: 'auto' }}>Chat with a skin specialist to discuss your goals and see what&rsquo;s possible.</p><Link to="/booking" className="btn-ref">Book Now <ArrowRight className="ml-2 h-5 w-5" /></Link></div></section><AnimatePresence>{current && selected !== null && <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-black/90 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={() => setSelected(null)}><button onClick={() => setSelected(null)} className="absolute top-6 right-6 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center hover:bg-white/20 transition-colors z-10"><X className="w-6 h-6 text-white" /></button><button onClick={(e) => { e.stopPropagation(); prev(); }} className="absolute left-4 md:left-8 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center hover:bg-white/20 transition-colors z-10"><ChevronLeft className="w-6 h-6 text-white" /></button><button onClick={(e) => { e.stopPropagation(); next(); }} className="absolute right-4 md:right-8 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center hover:bg-white/20 transition-colors z-10"><ChevronRight className="w-6 h-6 text-white" /></button><motion.div className="max-w-4xl mx-4" initial={{ scale: .9, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} exit={{ scale: .9, opacity: 0 }} onClick={(e) => e.stopPropagation()}><img src={current.image} alt={current.caption} className="w-full max-h-[80vh] object-contain" style={{ borderRadius: '2px' }} /></motion.div></motion.div>}</AnimatePresence></Layout>;
}
